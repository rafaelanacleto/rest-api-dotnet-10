# Arquivo de exemplo de variáveis de ambiente
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Ambiente da aplicação (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Production

# URLs que a aplicação irá escutar
ASPNETCORE_URLS=http://+:8080

# String de conexão do SQL Server
# Formato para Docker Compose (usando nome do serviço 'sqlserver')
ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=RestApiDb;User Id=sa;Password=Rafael.2024;TrustServerCertificate=True;

# Formato para Azure SQL Database (produção)
# ConnectionStrings__DefaultConnection=Server=tcp:seu-servidor.database.windows.net,1433;Database=RestApiDb;User Id=adminuser;Password=SuaSenha123!;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

# Configurações do SQL Server (para docker-compose)
ACCEPT_EULA=Y
MSSQL_PID=Developer
SA_PASSWORD=Rafael.2024

# Configurações de Logging
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft=Warning
Logging__LogLevel__Microsoft.AspNetCore=Warning

# Application Insights (opcional - para monitoramento no Azure)
# ApplicationInsights__InstrumentationKey=sua-chave-aqui

# CORS (se necessário configurar origins específicas)
# AllowedOrigins=https://seudominio.com,https://outro-dominio.com
